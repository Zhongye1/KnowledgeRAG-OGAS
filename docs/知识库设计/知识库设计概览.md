```
阶段 1：知识库管理（先做）
├── 知识库 CRUD（创建/重命名/归档/删除）
├── 权限模型（可见范围、上传/删除权限）
├── 文档元数据模型（文档属于哪个库、状态、版本）
└── 列表/详情/状态展示（即使先不接真摄取）

阶段 2：文档摄取流水线（后做）
├── 上传 → 挂到某个知识库
├── 异步任务（解析 → 清洗 → 分块 → 向量化）
├── 状态机（pending / parsing / indexing / ready / failed）
└── 失败重试、增量更新、删除联动清向量

阶段 3：联调与体验
├── 知识库详情页展示文档列表 + 摄取进度
├── 检索测试（按库隔离）
└── 问答时选择/限定知识库


┌─────────────────────────────────────────────────────────────┐
│                     知识库管理（容器层）                    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │ 知识库 A    │  │ 知识库 B    │  │ 知识库 C    │  ...     │
│  │ 权限/配置   │  │ 权限/配置   │  │ 权限/配置   │          │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘          │
│         │                │                │                 │
│         ▼                ▼                ▼                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │ 文档列表    │  │ 文档列表    │  │ 文档列表    │          │
│  │ 状态/版本   │  │ 状态/版本   │  │ 状态/版本   │          │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘          │
└─────────┼────────────────┼────────────────┼─────────────────┘
          │                │                │
          └────────────────┼────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────┐
│              文档摄取流水线（过程层，可异步/可重试）        │
│  上传 → 格式路由 → Loader → 清洗 → Splitter → Embed → 入库  │
│  （结果写回：文档状态、Chunk、向量，并归属到对应知识库）    │
└─────────────────────────────────────────────────────────────┘
```

---

## 相关 Spec

- 知识库管理环（阶段 1，已实现）：[2026-09-01-eaglerag-kb-migration-design.md](../specs/2026-09-01-eaglerag-kb-migration-design.md)
- 文档版本化（阶段 2，待 RAG 摄取/生成管线完成后实现）：[2026-09-02-document-versioning-spec.md](../specs/2026-09-02-document-versioning-spec.md)
